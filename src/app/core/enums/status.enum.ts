export enum PracticeStatus {
    InProgress = 'in_progress',
    Completed = 'completed',
    Pending = 'pending'
}

export enum StudentStatus {
    InProgress = 'in_progress',
    Completed = 'completed',
    Pending = 'pending'
}
